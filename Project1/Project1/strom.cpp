#include <stdio.h>

/*void fill(int amount, const char withWhat)
{
	while (amount> 0) {
		if (withWhat == ' ') {
			printf(" ");
		}
		else {
			printf("*");
		}
		--amount;
	}
}

void tree(int layers) 
{
	int width = 2 * layers + 1;
	int counter = 1;
	while (counter < width) 
	{
		fill((width - counter) / 2, ' ');
		fill(counter, '*');
		fill((width - counter) / 2, ' ');
		printf("\n");
		counter += 2;
	};
}*/